package com.logismart.infraestructura.estructural.abstractfactory;

/** Producto abstracto - calcula costos según la región. */
public interface CalculadorCostos {
    double calcularCosto(double peso);
}
